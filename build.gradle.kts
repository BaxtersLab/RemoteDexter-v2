// Root build for RemoteDexter-v2
// Minimal configuration; subprojects declare Kotlin plugin versions for clarity.

subprojects {
    repositories {
        mavenCentral()
    }
    group = "com.remotedexter"
    version = "0.1.0-SNAPSHOT"
}
